pretty-midi==0.2.9
omegaconf==2.1.1
transformers==4.16.1 
pytorch-lightning==1.8.4
note-seq==0.0.5 
pyFluidSynth==1.3.0
torch>=2.0.0
torchaudio>=2.0.0
# The "numpy<2" constraint was removed as it prevents installation on Python 3.13+.
# To ensure compatibility, install numpy>=2 first, then install the rest of the packages.
numpy>=2
yt-dlp>=2024.03.01
librosa
soundfile
numba
scipy
